const canvas = document.querySelector("canvas");
const contexte = canvas.getContext("2d");

/*Position x
 position y
taille en largeur
taille en hauteur

(function draw(){
    contexte.fillStyle = "#BADA55";
    contexte.fillRect(100,100,50,50);
}) ();


(function drawcircle(){
    contexte.fillStyle = "red";
    contexte.beginPath();
    contexte.arc(40,40,25, 0,Math.PI * 1.5, false); 
    contexte.fill();

})();

*/